package Project_Mod;

//#include "stdio.h"
//import * from 'Mod'


class User{
    int age(){
        return 0;
    }
}
int sayhi(){
    User@ u = new User()
    int i = 0;
    u.age();
    bool a =  i > u.age() && i < u.age() || i == u.age() && i - 1 == u.age() + 1
}